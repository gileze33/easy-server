{
  "name": "easy-server",
  "version": "2.0.0",
  "description": "Easiest possible express wrapper to get something out the door yesterday",
  "main": "index.js",
  "scripts": {
    "build": "tsc -p .",
    "watch": "tsc -w -p .",
    "typings": "typings -v > /dev/null && typings install || echo 'Please install the `typings` package to use this package with typescript'",
    "postinstall": "[ \"$NODE_ENV\" == \"production\" ] || npm run -s typings",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/gileze33/easy-server"
  },
  "author": "Giles Williams",
  "license": "ISC",
  "dependencies": {
    "debug": "^2.1.0",
    "express": "^4.10.6",
    "middleware-manager": "0.0.3",
    "require-all": "^2.0.0"
  },
  "devDependencies": {
    "tslint": "^3.3.0",
    "typescript": "^1.7.5"
  }
}
